// const express = require("express");
// const router = express.Router();
// const passwordResetController = require("../controllers/Common/passwordResetController");

// // Route for requesting a password reset email
// router.post("/forgot-password", passwordResetController.forgotPassword);

// // Route for verifying reset token is valid
// router.get("/reset-password/:token", passwordResetController.verifyResetToken);

// // Route for resetting the password with a valid token
// router.post("/reset-password/:token", passwordResetController.resetPassword);

// module.exports = router;
